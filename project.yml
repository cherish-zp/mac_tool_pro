name: mac_tool_pro
options:
  bundleIdPrefix: com.zp.mac-tool-pro
  deploymentTarget:
    macOS: "13.0"
settings:
  base:
    SWIFT_VERSION: "5"
    MACOSX_DEPLOYMENT_TARGET: "13.0"
    ARCHS: arm64
targets:
  mac_tool_pro:
    type: application
    platform: macOS
    sources:
      - path: Sources/App
      - path: Sources/Shared
    entitlements:
      path: Resources/App.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zp.mac-tool-pro
        PRODUCT_NAME: mac_tool_pro
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Resources/App-Info.plist
    dependencies:
      - target: FinderSyncExt
        embed: true
  FinderSyncExt:
    type: app-extension
    platform: macOS
    sources:
      - path: Sources/FinderSyncExt
      - path: Sources/Shared
    entitlements:
      path: Resources/Ext.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zp.mac-tool-pro.FinderSyncExt
        PRODUCT_NAME: FinderSyncExt
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Resources/Ext-Info.plist
